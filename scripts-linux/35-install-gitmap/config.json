{
  "install": {
    "method":      "curlOneLiner",
    "scriptName":  "gitmap",
    "installUrl":  "https://github.com/alimtvnetwork/gitmap-v9/releases/download/v3.180/install.sh",
    "binDir":      "${HOME}/.local/bin"
  },
  "verify": {
    "binary": "${HOME}/.local/bin/gitmap --version"
  }
}
